# 프로젝트 개요

모바일에서 보기 좋은 반응형 초대장(청첩장/돌잔치/행사 등) 웹페이지 한 장짜리 프로젝트. **HTML, CSS, JavaScript만 사용**하며 별도의 빌드 도구나 프레임워크(React, Vue 등)는 쓰지 않는다.

## 핵심 기능

1. **D-Day 카운트다운** — 행사일까지 남은 시간(일/시/분/초 또는 일 단위)을 계산해서 실시간으로 보여준다.
2. **장소 지도** — 예식/행사 장소를 지도로 보여주고, 지도 탭 시 길찾기(카카오맵/네이버지도/애플지도 등)로 연결되는 딥링크를 제공한다.

## 기술 스택 & 제약

- 순수 HTML/CSS/JS. 빌드 도구(webpack, vite 등) 사용 안 함.
- 외부 라이브러리 사용은 최소화한다. 지도 표시를 위한 CDN 스크립트(카카오맵 JS SDK, 네이버지도 API 등)는 예외적으로 허용하되, 어떤 방식을 쓸지는 plan 단계에서 사용자와 함께 정하고 spec.md에 명시한다.
  - API 키가 필요한 지도 SDK를 쓰는 경우, 키 발급/설정은 사용자에게 안내만 하고 대신 입력하지 않는다.
  - 외부 SDK 없이 정적 지도 이미지 + 길찾기 딥링크만으로 구현하는 방안도 우선적으로 고려한다(의존성이 적고 로딩이 가벼움).
- 모바일 퍼스트로 설계한다. 실제 사용 시나리오는 대부분 카카오톡 등으로 공유된 링크를 스마트폰에서 여는 것이므로, 작은 화면 레이아웃을 기준으로 만들고 데스크톱은 보조로 대응한다.
- 접근성: 대체 텍스트, 충분한 명도 대비, 터치 타겟 크기를 신경 쓴다.
- 개인정보(이름, 연락처, 계좌번호 등)가 페이지에 들어갈 수 있으므로, 소스에 하드코딩된 개인정보는 커밋 전에 사용자에게 다시 한번 확인한다.

## 파일 구조

```
invitation/
  CLAUDE.md
  spec.md          # plan 단계 산출물
  review.md         # review 단계 산출물
  index.html
  css/
    style.css
  js/
    script.js       # D-Day 계산, 지도 초기화 등
  assets/           # 이미지, 폰트 등
```

## 작업 사이클

사용자가 요구사항(행사 종류, 날짜, 장소, 디자인 톤 등)을 주면 다음 순서로 진행한다.

1. **Plan** — spec.md를 작성한다. 다음 내용을 포함한다: 행사 정보(어떤 행사인지, 날짜/시간, 장소), 페이지 섹션 구성(인사말, D-Day, 갤러리, 지도, 연락처/계좌 등), 디자인 톤(색상·폰트 방향), 지도 구현 방식(SDK vs 정적 이미지+딥링크). **사용자 승인 전에는 코드를 작성하지 않는다.**
2. **Build** — 승인된 spec.md대로 구현한다.
3. **Review** — 브라우저에서 실제로 동작을 확인한다(D-Day 카운트다운이 정확히 계산되는지, 지도/길찾기 링크가 열리는지, 모바일 화면에서 레이아웃이 깨지지 않는지). review.md에 결과를 기록하고, 문제가 있으면 수정한다.
4. **완료 보고** — 무엇을 만들었는지, 남은 할 일(API 키 발급, 실제 이미지 교체 등)이 있으면 사용자에게 안내한다.

이 프로젝트는 페이지가 사실상 하나(초대장 한 장)이므로, 각 단계를 반드시 별도 서브에이전트로 나눌 필요는 없다. 다만 작업 범위가 커지면(예: 여러 테마 변형, RSVP 폼 추가 등) plan/build/review를 서브에이전트로 분리해도 된다.

## 규칙

- 승인 없이 구현을 시작하지 않는다.
- 막히면(예: 지도 API 키 필요, 디자인 방향 불명확) 사용자에게 물어본다.
- 실제 행사 정보(날짜, 장소, 개인정보)는 추측하지 말고 사용자에게 확인한다.
